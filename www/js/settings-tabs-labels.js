function settingTabLabels() {
  return {
    device: t.device || "Device",
    network: t.network,
    time: t.timeDate,
    integrations: t.integrations,
    auth: t.auth,
    files: t.files,
  };
}
